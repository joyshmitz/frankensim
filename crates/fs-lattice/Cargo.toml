[package]
name = "fs-lattice"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Lattice/infill optimization (plan 9.5): periodic unit-cell homogenization with physics-bound audits, graded macro-optimization through fitted homogenized laws, and de-homogenization back to micro-geometry with full-resolution re-analysis. 2D smoke tier."

[package.metadata.frankensim]
layer = "L4"

[dependencies]
fs-cutfem = { path = "../fs-cutfem" }
fs-la = { path = "../fs-la" }
fs-material = { path = "../fs-material" }
fs-solid = { path = "../fs-solid" }
fs-sparse = { path = "../fs-sparse" }

[lints]
workspace = true
