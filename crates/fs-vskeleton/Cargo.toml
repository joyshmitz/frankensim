[package]
name = "fs-vskeleton"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "PV vertical skeleton: SDF -> PDE -> objective -> adjoint check -> optimize -> ledger replay."

[package.metadata.frankensim]
layer = "L6"

[dependencies]
fs-qty = { path = "../fs-qty" }
fs-obs = { path = "../fs-obs" }
fsqlite = { path = "../../../frankensqlite/crates/fsqlite" }

[dev-dependencies]

[lints]
workspace = true
