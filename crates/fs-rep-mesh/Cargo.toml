[package]
name = "fs-rep-mesh"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Mesh charts: half-edge surfaces, oriented tet/hex complexes, generalized-winding-number repair, watertight queries."

[package.metadata.frankensim]
layer = "L2"

[dependencies]
fs-alloc = { path = "../fs-alloc" }
fs-evidence = { path = "../fs-evidence" }
fs-exec = { path = "../fs-exec" }
fs-geom = { path = "../fs-geom" }
fs-obs = { path = "../fs-obs" }

[dev-dependencies]
asupersync = { path = "../../../asupersync" }

[lints]
workspace = true
