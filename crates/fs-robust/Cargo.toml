[package]
name = "fs-robust"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Objective epistemics (addendum Proposal F): apply the three colors to the GOAL itself. Colored objectives over (design, environment), CVaR/robust optima, the weakest-input color rule on headlines, colored fragility curves, and the amended optimization contract that refuses to optimize an un-colored objective."

[package.metadata.frankensim]
layer = "L4"

[dependencies]
fs-evidence = { path = "../fs-evidence" }

[dev-dependencies]

[lints]
workspace = true
