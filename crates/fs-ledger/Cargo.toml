[package]
name = "fs-ledger"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Design Ledger v0: content-addressed artifacts, event-sourced ops, lineage, metrics, tune cache, events on FrankenSQLite."

[package.metadata.frankensim]
layer = "L6"

[dependencies]
fs-blake3 = { path = "../fs-blake3" }
fs-evidence = { path = "../fs-evidence" }
fs-exec = { path = "../fs-exec" }
fs-qty = { path = "../fs-qty" }
fs-regime = { path = "../fs-regime" }
fsqlite = { path = "../../../frankensqlite/crates/fsqlite" }

[dev-dependencies]
fsqlite = { path = "../../../frankensqlite/crates/fsqlite", features = ["async-api"] }

[lints]
workspace = true
