[package]
name = "fs-roofline"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Roofline harness: measured machine axes, kernel attainment vs falsifiable targets, ledgered with fingerprints."

[package.metadata.frankensim]
layer = "L6"

[dependencies]
fs-blake3 = { path = "../fs-blake3" }
fs-exec = { path = "../fs-exec" }
fs-session = { path = "../fs-session" }
fs-substrate = { path = "../fs-substrate" }
fs-simd = { path = "../fs-simd" }
fs-ledger = { path = "../fs-ledger" }

[dev-dependencies]
fs-plan = { path = "../fs-plan" }

[lints]
workspace = true
