[package]
name = "fs-truss-e2e"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "TrussPath: outward-certified truss optimum bounds plus an advisory endpoint-checked tropical load path."

[package.metadata.frankensim]
layer = "L4"

[dependencies]
fs-truss = { path = "../fs-truss" }
fs-tropical = { path = "../fs-tropical" }
fs-evidence = { path = "../fs-evidence" }
fs-exec = { path = "../fs-exec" }
fs-ivl = { path = "../fs-ivl" }

[dev-dependencies]
fs-alloc = { path = "../fs-alloc" }
fs-sparse = { path = "../fs-sparse" }

[lints]
workspace = true
