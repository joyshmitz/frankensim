[package]
name = "fs-topo"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Validity and topology certificates: manifoldness with defect localization, exact self-intersection proofs, cubical Betti numbers and 0-dim persistence."

[package.metadata.frankensim]
layer = "L2"

[dependencies]
fs-exec = { path = "../fs-exec" }
fs-geom = { path = "../fs-geom" }
fs-ivl = { path = "../fs-ivl" }
fs-obs = { path = "../fs-obs" }
fs-rep-mesh = { path = "../fs-rep-mesh" }

[dev-dependencies]
asupersync = { path = "../../../asupersync" }
fs-alloc = { path = "../fs-alloc" }
fs-rep-frep = { path = "../fs-rep-frep" }

[lints]
workspace = true
